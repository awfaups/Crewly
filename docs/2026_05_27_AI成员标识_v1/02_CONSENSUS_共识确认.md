# AI 成员标识 - 共识确认

## 已确认

- AI 成员姓名旁显示 `AI` 徽标。
- 人类成员不显示该徽标。
- 侧栏、消息、任务看板、右侧详情都需要可见。
- 使用复用组件，避免样式分散。

## 不做

- 不修改成员数据。
- 不新增过滤或权限逻辑。
- 不改变头像和模型配置。

## 验收口径

- 侧栏 AI 成员姓名旁可见 `AI`。
- 消息流 AI 作者姓名旁可见 `AI`。
- 任务看板 AI 负责人姓名旁可见 `AI`。
- 右侧 AI 成员详情姓名旁可见 `AI`。
- `npm run lint` 和 `npm run build` 通过。
