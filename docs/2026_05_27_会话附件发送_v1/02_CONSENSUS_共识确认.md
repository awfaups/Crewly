# 会话附件发送 - 共识确认

## 决策

- 本轮采用浏览器 `FileReader` 将附件转为 data URL。
- 附件数据保存在 localStorage 中，仅用于 demo。
- 为避免本地存储过大，单个附件限制为 2MB。
- 支持一次选择多个附件。
- 附件类型以浏览器提供的 MIME type 为准。

## 风险

- data URL 会放大存储体积，不适合生产。
- localStorage 容量有限，大文件可能保存失败。
- 后续接后端时需要替换为真实上传和对象存储。
