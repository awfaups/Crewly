# Crewly 后端基础设施 - 待办清单

- [x] 安装后端依赖
- [x] 增加 Fastify 服务入口
- [x] 增加 Drizzle schema
- [x] 生成数据库迁移
- [x] 增加核心读取 API
- [x] 增加环境变量模板
- [x] 完成最终验证和部署

## 后续建议

- 增加 seed 脚本，将当前 mock 数据导入数据库。
- 增加 `POST /messages`、`POST /tasks`、`PATCH /approvals/:id`。
- 增加服务端 AI 调用代理。
- 增加 SSE runtime event stream。
