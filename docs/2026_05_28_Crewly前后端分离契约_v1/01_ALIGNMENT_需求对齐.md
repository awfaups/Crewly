# Crewly 前后端分离契约 - 需求对齐

## 背景

用户确认要继续推进前后端分离。当前仓库已经有 `src/` 前端和 `server/` 后端，但缺少共享契约、写入 API 和前端数据源开关。

## 本轮目标

- 新增 `shared/` 目录承载 API DTO 和共享契约。
- 后端补齐消息、任务、审批三个关键写入接口。
- 前端新增 API client 和数据源模式开关。
- 前端默认保持本地演示，不强制依赖后端。

## 非目标

- 不把前端全面切到远程 API。
- 不实现登录鉴权。
- 不实现数据库 seed。
- 不部署独立后端服务。
